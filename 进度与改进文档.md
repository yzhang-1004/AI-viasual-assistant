# 「心灵树洞」项目进度与改进清单

> 文档生成时间：2026-06-12  
> 项目路径：`d:\AIprogram2.0\mind-tree-hole\`  
> 运行地址：`http://localhost:5173/`  
> 源文件：22 个 `.ts/.tsx/.css` 文件

---

## 一、总体完成度

按设计文档 5 大维度评估：

| 维度 | 完成度 | 说明 |
|------|:--:|------|
| 界面与交互设计（UI/UX） | **85%** | 核心布局+动效已就绪，缺画面亮度自适应 |
| 四大功能模块 | **90%** | Prompt+交互已就绪，冥想呼吸圈+每日卡片已实现 |
| 端云协同与成本控制 | **45%** | L0+L2 已实现，缺 L1/L3、视觉降维、TTS缓存、摘要压缩 |
| 极速响应体验 | **70%** | 流式链路+打断+预测预填已实现，缺灵积TTS流式 |
| 灵积大模型集成 | **60%** | LLM流式✅，ASR/TTS仍用浏览器API |

**综合完成度：约 70%**（核心体验闭环已打通）

---

## 二、已实现功能清单（22项）

| # | 功能 | 对应设计章节 | 实现文件 |
|---|------|:--:|------|
| 1 | 全屏沉浸式通话容器 | §2.2 | `FullScreenCall.tsx` |
| 2 | 暖色暗调背景 + 萤火虫粒子 | §2.1 | `FireflyParticles.tsx`, `index.css` |
| 3 | Canvas 呼吸光晕（倾听/思考/说话三态） | §2.1 | `BreathingGlow.tsx` |
| 4 | 四种模式切换栏（顶部） | §2.2, §3 | `ModeSwitchBar.tsx` |
| 5 | 用户摄像小窗（左上角+可隐藏） | §2.2 | `UserCameraWindow.tsx` |
| 6 | AI 回复文字区（打字机+淡入） | §2.2 | `AIReplyText.tsx` |
| 7 | 底部操控栏（麦克风+摄像头+文字+设置+挂断） | §2.2 | `BottomControlBar.tsx` |
| 8 | 声波纹动画（5条波柱） | §2.3 | `MicButton.tsx`, `index.css` |
| 9 | 浏览器 SpeechRecognition 语音输入 | §5 | `speech.ts`, `useConversation.ts` |
| 10 | 浏览器 SpeechSynthesis TTS 朗读 | §5 | `speech.ts`, `useConversation.ts` |
| 11 | 文字输入备选（气泡按钮） | §2.2 | `TextInput.tsx` |
| 12 | 灵积 Qwen-Plus 流式 SSE 调用 | §4, §5 | `llm.ts` |
| 13 | L0 规则引擎（问候/语气词 0 成本） | §4.3 | `prompts.ts` → `getL0Response()` |
| 14 | 四种模式 System Prompt | §3 | `prompts.ts` → `BASE_PROMPTS` |
| 15 | 打断机制（AbortController + 停止 TTS） | §5.3 | `useConversation.ts` → `interruptAI()` |
| 16 | 预测性预填（0.8s 静音触发） | §5.1 | `useConversation.ts` → silenceTimer |
| 17 | 对话上下文管理（Zustand） | §4.4 | `appStore.ts` → `useConversationStore` |
| 18 | 冥想呼吸圈（4-7-8 Canvas 动画） | §3.3 | `BreathingCircle.tsx` |
| 19 | 每日力量治愈卡片 | §3.4 | `DailyPowerCard.tsx` |
| 20 | 设置面板（API Key 管理） | - | `SettingsPanel.tsx` |
| 21 | 摄像头 getUserMedia 接入 | - | `UserCameraWindow.tsx` |
| 22 | 光晕颜色随模式变化 | §3.2 | `BreathingGlow.tsx` → `getParams()` |

---

## 三、对照设计文档的差距分析

### 3.1 界面与交互 —— 差 1 项

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| ~~用户的话不显示~~ | ✅ | 已实现：语音模式下界面不显示用户文字 |
| ~~声波纹动画~~ | ✅ | 已实现：5 条动态波柱 |
| 画面亮度自适应 | ❌ | **缺失**：未检测环境亮度调整 UI 整体亮度 |
| ~~说完后文字隐去~~ | ⚠️ | **部分**：文字显示正确，但缺少 3 秒后自动淡出的定时器 |

### 3.2 功能模块 —— 差 1 项

| 模块 | 状态 | 说明 |
|------|:--:|------|
| 陪伴倾听 | ✅ | Prompt + 光晕柔和态，已测试通过 |
| 解惑指路 | ✅ | CBT Prompt + 光晕森绿调，已测试通过 |
| 冥想放松 | ✅ | 呼吸圈 Canvas + 粒子减速 + Prompt |
| 每日力量 | ✅ | 卡片 UI + Prompt |
| 模式切换联动光晕 | ✅ | 颜色/频率/涟漪随模式变化 |

### 3.3 端云协同 —— 差 6 项（核心差距）

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| L0 规则引擎 | ✅ | 已实现 |
| L1 端侧小模型 | ❌ | 未实现，当前直接从 L0 跳到 L2 |
| L2 云端文本 LLM | ✅ | 已实现 Qwen-Plus 流式 |
| L3 云端多模态 | ❌ | 未实现 Qwen-VL 调用 |
| 端侧视觉分析（亮度/表情） | ❌ | 未实现 Canvas 截图+亮度分析 |
| 视觉特征转文本 | ❌ | 未实现视觉上下文拼入 Prompt |
| 语气词过滤（ASR 层面） | ⚠️ | L0 已做匹配，但 ASR 识别结果中不丢弃 |
| 滑动窗口摘要（>10轮） | ⚠️ | 上下文管理已实现，但 `callLLM()` 摘要函数未被调用 |
| TTS 高频话术缓存 | ❌ | 未实现 50 句预生成音频 |

### 3.4 极速响应 —— 差 2 项

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| 预测性预填 | ✅ | 0.8s 静音自动触发 |
| LLM 流式 | ✅ | SSE 逐 token 解析 |
| 打断机制 | ✅ | AbortController + stopSpeaking |
| 灵积 CosyVoice 流式 TTS | ❌ | 当前用浏览器 TTS，延迟高、音质差 |
| 灵积 Fun-ASR WebSocket | ❌ | 当前用浏览器 SpeechRecognition |

### 3.5 灵积集成 —— 差 2 项

| 服务 | 状态 | 说明 |
|------|:--:|------|
| 文本生成（Qwen-Plus） | ✅ | OpenAI 兼容接口，SSE 流式 |
| 语音识别（Fun-ASR） | ❌ | 未接入 WebSocket |
| 语音合成（CosyVoice） | ❌ | 未接入 WebSocket |
| 多模态视觉（Qwen-VL） | ❌ | 未接入 |

---

## 四、待改进清单（按优先级排列）

### 🔴 P0 — 体验缺陷（建议优先修复）

| # | 问题 | 影响 | 修复方案 | 预估 |
|---|------|------|---------|:--:|
| 1 | AI 说完后文字不自动消失 | 违反"保持画面干净"设计 | 在 `AIReplyText.tsx` 中 AI 状态变回 listening 后 3s 定时 `setAiReplyText('')` | 10min |
| 2 | 浏览器 TTS 音质差、延迟高 | 体验远不如设计预期 | 接入灵积 CosyVoice WebSocket 流式 TTS | 3h |
| 3 | 缺少画面亮度自适应 | 设计亮点未实现 | 从摄像头 canvas 截帧计算平均亮度，动态调整 CSS 变量 | 1.5h |

### 🟡 P1 — 功能增强

| # | 功能 | 说明 | 预估 |
|---|------|------|:--:|
| 4 | 视觉上下文注入 | 每 2s 截帧检测亮度→生成 `[视觉上下文：环境昏暗]`→拼入 Prompt | 2h |
| 5 | 滑动窗口自动摘要 | 对话 >10 轮时调用 `callLLM()` 压缩前 9 轮为摘要 | 1h |
| 6 | 灵积 Fun-ASR WebSocket | 替换浏览器 SpeechRecognition，提升识别准确率 | 3h |
| 7 | TTS 高频话术缓存 | 预生成 50 句音频到 `public/tts-cache/`，L0 命中时 0 延迟播放 | 2h |

### 🟢 P2 — 远期规划

| # | 功能 | 说明 | 预估 |
|---|------|------|:--:|
| 8 | L3 多模态 Qwen-VL | 当用户说"你看看"时截帧发图片给 Qwen-VL | 2h |
| 9 | 端侧表情检测 | 使用 FaceDetector API 或 TensorFlow.js | 4h |
| 10 | AI 打断灵敏度可调 | 设置面板添加 VAD 灵敏度滑块 | 1h |
| 11 | 对话历史导出/清除 | 遵守数据隐私，给用户数据控制权 | 1h |
| 12 | 移动端响应式适配 | 当前仅适配桌面端 | 3h |

---

## 五、项目文件结构一览

```
mind-tree-hole/src/
├── App.tsx                          (9行) 根组件
├── main.tsx                         (10行) 入口
├── index.css                        (160行) 全局样式+动画
├── config/
│   └── dashscope.ts                 (34行) 灵积端点+Key管理
├── types/
│   └── index.ts                     (56行) 类型定义
├── stores/
│   └── appStore.ts                  (50行) Zustand 状态+对话上下文
├── services/
│   ├── llm.ts                       (135行) 灵积LLM流式+非流式
│   └── speech.ts                    (128行) 浏览器ASR/TTS封装
├── utils/
│   └── prompts.ts                   (81行) 四种模式Prompt+L0规则
├── hooks/
│   └── useConversation.ts           (251行) 核心对话编排
└── components/
    ├── layout/
    │   ├── FullScreenCall.tsx        (73行) 全屏容器
    │   ├── ModeSwitchBar.tsx         (38行) 模式切换
    │   └── BottomControlBar.tsx      (114行) 底部操控栏
    ├── ai-avatar/
    │   ├── BreathingGlow.tsx         (121行) Canvas呼吸光晕
    │   └── FireflyParticles.tsx      (42行) 萤火虫粒子
    ├── video/
    │   └── UserCameraWindow.tsx      (59行) 摄像小窗
    ├── interaction/
    │   ├── MicButton.tsx             (62行) 麦克风+声波纹
    │   ├── AIReplyText.tsx           (51行) AI回复文字区
    │   ├── BreathingCircle.tsx       (104行) 冥想呼吸圈
    │   └── TextInput.tsx             (78行) 文字输入
    ├── cards/
    │   └── DailyPowerCard.tsx        (56行) 每日力量卡片
    └── settings/
        └── SettingsPanel.tsx         (124行) 设置面板

总计 22 个源文件，约 1,600 行代码
```

---

## 六、技术债务记录

| # | 债务 | 位置 | 说明 |
|---|------|------|------|
| 1 | 浏览器 Speech API 仅 Chrome 支持 | `speech.ts` | Firefox/Safari 语音识别不可用 |
| 2 | API Key 硬编码在源码 | `dashscope.ts:2` | 生产环境需后端代理 |
| 3 | `getL0Response` 规则覆盖有限 | `prompts.ts:62` | 仅 4 条正则，可扩展 |
| 4 | 粒子动画对低端设备可能有性能压力 | `FireflyParticles.tsx` | 40 个 CSS animation 粒子 |
| 5 | `useConversation` 依赖链较深 | `useConversation.ts` | 后续可拆分为多个独立 hook |

---

## 七、下次开发建议

如果按 **2-3 小时** 的改进窗口，建议优先做：

1. **P0-1**：AI 说话后文字自动隐去（10 分钟）
2. **P0-3**：画面亮度自适应（1.5 小时）
3. **P1-5**：滑动窗口自动摘要（1 小时）

如果按 **完整一天** 的改进窗口，建议追加：

4. **P0-2**：灵积 CosyVoice 流式 TTS 接入（3 小时）
5. **P1-4**：视觉上下文注入（2 小时）
6. **P1-6**：灵积 Fun-ASR WebSocket 接入（3 小时）

---

> **状态**：核心体验闭环已打通，可以实际使用。P0 项修复后体验将大幅提升。
