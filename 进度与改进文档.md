# 「心灵树洞」项目进度与改进清单（更新于 2026-06-14）

> 项目路径：`d:\AIprogram\mind-tree-hole\`
> 运行地址：`http://localhost:5174/`
> 源文件：23 个 `.ts/.tsx/.css` 文件，约 2,300 行代码

---

## 一、总体完成度

| 维度 | 修改前 | 修改后 | 说明 |
|------|:--:|:--:|------|
| 界面与交互设计（UI/UX） | 85% | **90%** | 新增 AI 打断按钮 |
| 四大功能模块 | 90% | **90%** | 无变化 |
| 端云协同与成本控制 | 45% | **65%** | 视觉上下文注入✅ |
| 极速响应体验 | 70% | **90%** | CosyVoice 流式 TTS✅ + 打断按钮✅ |
| 灵积大模型集成 | 60% | **80%** | CosyVoice TTS✅，ASR 仍缺 |

**综合完成度：约 83%**（↑ 从 70%）

---

## 二、本轮已实现的新功能（6 项）

| # | 功能 | 文件 | 说明 |
|---|------|------|------|
| 23 | **语音回路修复** | `useConversation.ts` | TTS 朗读期间暂停语音识别，防止 AI 把自己说的话当用户输入 |
| 24 | **AI 打断按钮** | `MicButton.tsx` | AI 说话时麦克风按钮变红色方块，点击立即停止 TTS+恢复倾听 |
| 25 | **视觉上下文注入（二期）** | `useVisionContext.ts` | 端侧 face-api.js 人脸检测+表情+亮度→生成 `[视觉上下文：…]` 注入 Prompt |
| 26 | **CosyVoice 流式 TTS** | `tts.ts` | WebSocket 接入灵积 CosyVoice，边生成边播放，降级回浏览器 TTS |
| 27 | **Prompt 视觉能力感知** | `prompts.ts` | 四模式 Prompt 均告知 AI 可"看到"用户，避免说"我看不到你" |
| 28 | **摄像头 ref 暴露** | `UserCameraWindow.tsx` | forwardRef 改造，供 useVisionContext 截帧分析 |

---

## 三、新增/修改文件清单

| 文件 | 状态 | 行数 |
|------|:--:|------|
| `src/services/tts.ts` | **新建** | 182 行 |
| `src/hooks/useVisionContext.ts` | **新建** | 159 行 |
| `src/hooks/useConversation.ts` | 修改 | 251→319 行 |
| `src/utils/prompts.ts` | 修改 | 81→87 行 |
| `src/components/interaction/MicButton.tsx` | 修改 | 62→86 行 |
| `src/components/video/UserCameraWindow.tsx` | 修改 | 59→74 行 |
| `src/components/layout/FullScreenCall.tsx` | 修改 | 73→80 行 |
| `package.json` | 修改 | 新增 face-api.js + @tensorflow/tfjs |

---

## 四、对照设计文档的差距分析（更新）

### 4.1 界面与交互

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| 用户的话不显示 | ✅ | |
| 声波纹动画 | ✅ | |
| AI 说话后文字自动隐去 | ❌ | **仍缺失**：文字在 AI 说完后不消失 |
| 画面亮度自适应 | ❌ | **仍缺失**：视觉信息已注入 Prompt，但 UI 亮度未联动 |

### 4.2 端云协同

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| L0 规则引擎 | ✅ | |
| L1 端侧小模型 | ❌ | 未实现 |
| L2 云端文本 LLM | ✅ | Qwen-Plus 流式 |
| L3 云端多模态 | ❌ | 未实现 |
| 端侧视觉分析 | ✅ | **本轮实现**：亮度+人脸+表情 |
| 视觉特征转文本注入 Prompt | ✅ | **本轮实现** |
| 滑动窗口摘要（>10轮） | ❌ | 未实现 |
| TTS 高频话术缓存 | ❌ | 未实现 |

### 4.3 极速响应

| 设计要求 | 状态 | 说明 |
|---------|:--:|------|
| 预测性预填 | ✅ | |
| LLM 流式 | ✅ | |
| 打断机制 | ✅ | **本轮增强**：按钮打断 + TTS 打断联动 |
| CosyVoice 流式 TTS | ✅ | **本轮实现** |
| Fun-ASR WebSocket | ❌ | 仍用浏览器 SpeechRecognition |

---

## 五、待改进清单（更新）

### 🔴 P0 — 体验缺陷

| # | 问题 | 状态 | 说明 |
|---|------|:--:|------|
| 1 | AI 说完后文字不自动消失 | ❌ | 违反"保持画面干净"设计，AI 停止说话后 3s 调用 `setAiReplyText('')` |
| 2 | 浏览器 TTS 音质差 | ✅ 已修复 | CosyVoice 流式接入，自动降级 |
| 3 | 画面亮度自适应 | ❌ | UI 亮度未根据环境光联动 |

### 🟡 P1 — 功能增强

| # | 功能 | 状态 | 说明 |
|---|------|:--:|------|
| 4 | 视觉上下文注入 | ✅ 已实现 | face-api.js 端侧分析 |
| 5 | 滑动窗口自动摘要 | ❌ | >10 轮对话压缩 Token |
| 6 | Fun-ASR WebSocket | ❌ | 替换浏览器 ASR |

### 🟢 P2 — 远期

| # | 功能 | 状态 |
|---|------|:--:|
| 7 | L3 Qwen-VL 多模态 | ❌ |
| 8 | 移动端响应式 | ❌ |

---

## 六、下一步建议

按优先级排序，建议接下来做：

1. **P0-1（10 分钟）**：AI 说完后文字 3 秒自动消失 → 体验立竿见影
2. **P0-3（1 小时）**：画面亮度联动 UI 亮度 → 设计文档明确要求的亮点
3. **P1-5（1 小时）**：滑动窗口摘要 → 控制 Token 成本，防止长时间对话费用飙升

如果还有余力：
4. **P1-6（2-3 小时）**：Fun-ASR WebSocket → 提升识别准确率
